<!--
Rendered by /start into a real CLAUDE.md, once, during first-run (§2.1 step 11).
Every {{...}} placeholder below is replaced with this project's locked-in Q1–Q12
answers — an agent reading the rendered file should never see a placeholder or a
pattern this project didn't choose. Re-running /start on an already-initialized
project leaves this file alone unless the developer explicitly confirms a
conflicting change (§2.4).
-->
# {{APP_NAME}}

{{APP_NAME}} is a {{UI_FRAMEWORK}} iOS app targeting iOS {{MIN_IOS_VERSION}}+,
built with **{{ARCHITECTURE_PATTERN}}**, **{{NAVIGATION_APPROACH}}** navigation,
and **{{TOPOLOGY_TIER}}** modularization.

## Stack

| | |
|---|---|
| Language | {{LANGUAGE}} |
| UI framework | {{UI_FRAMEWORK}} |
| Architecture | {{ARCHITECTURE_PATTERN}} |
| Navigation | {{NAVIGATION_APPROACH}} |
| Networking | {{NETWORKING_STACK}} |
| Dependency injection | {{DI_STYLE}} |
| Testing | {{TESTING_FRAMEWORK}} |
| Project generation | {{TOOLING}} |
| Min iOS version | {{MIN_IOS_VERSION}} |
| Local persistence | {{PERSISTENCE}} — {{PERSISTENCE_NOTE}} |
<!--
PERSISTENCE_NOTE is one line naming what the answer means for generated code:
  SwiftData/Core Data → "every feature's Repository/Worker/Service takes a
    <Feature>LocalStore alongside its remote dependency; remote is the source of
    truth, the cache is the fallback (docs/ai/architecture.md)"
  None → "features are remote-only — no local store is generated, and adding one
    later is a Q4 change, not a per-feature decision"
-->

## Topology: {{TOPOLOGY_TIER}}

{{MODULE_GRAPH_SUMMARY}}
<!-- e.g. "One app target, no package boundary." at T1, or a module table at T2/T3. -->

Full module graph and ownership rules: `docs/ai/modularization.md`.
Full architecture layer shapes: `docs/ai/architecture.md`.

## Rules that apply everywhere

- **Never hardcode a color.** Every color/font is a Swift token in a `Theme/`
  directory — see `docs/ai/theming_rules.md`. `Scripts/check_hardcoded_colors.sh`
  blocks a commit that violates this.
- **Never hardcode a user-facing string.** Route through the owning module's
  `L10n.<key>` — see `docs/ai/ui_rules.md`.
- **Never `print()`.** Log through `Log.swift`'s `Logging` protocol (in the
  `Core` module, or `Logging` if this project extracted it) — SwiftLint's
  `no_print_statements` blocks the alternative. Never log a token, credential or
  personal data.
- **Never hardcode a base URL, endpoint host, or API key.** They come from
  `Secrets.xcconfig` (gitignored) → the app target's `Info.plist` → the
  composition root, which injects the resolved `URL` into `RequestBuilder`. A
  `Service` never builds a URL from a literal — see `docs/CODING_STANDARDS.md`.
  (Not applicable if this project's `networking` is `none`: there is no
  `RequestBuilder` and no `APIBaseURL` key. Don't add either to "fix" a build —
  adding a network call to an offline app is a decision for the developer.)
- **Credentials go in the Keychain.** Never a token, refresh token or password
  in `UserDefaults` or the local database — both are plaintext in the app
  container and land in an unencrypted backup. `UserDefaults` is for small
  non-sensitive app state; the local store is for domain records
  (`docs/CODING_STANDARDS.md`).
- **A protocol is owned by its consumer, not its implementer** — see
  `docs/CODING_STANDARDS.md`'s protocol-boundary section (§8.2).
- **Requirements live in `docs/product/`, and they change.** Before implementing
  a feature, read the PRD/SRS/API document that describes it — at the start of
  that task, from the file itself. This file deliberately carries a pointer and
  never a summary: a digest of a living document goes stale as fast as the
  document changes. If the requirement you need isn't there or contradicts
  itself, ask — don't invent one.
- **Use the Skills for routine work** — `/new-feature`, `/add-module`,
  `/add-permission`, etc. (`.claude/skills/`) — rather than hand-rolling folder
  structure or navigation wiring; they encode this project's locked-in decisions
  so you don't have to re-derive them.

## Where deeper rules live

| Topic | File |
|---|---|
| Product context — PRD, SRS, API contracts | `docs/product/` (living documents, re-read per task) |
| Architecture layer shapes | `docs/ai/architecture.md` |
| Module graph & ownership | `docs/ai/modularization.md` |
| Theming | `docs/ai/theming_rules.md` |
| UI conventions | `docs/ai/ui_rules.md` |
| Permissions | `docs/ai/permissions_rules.md` |
| Coding standards | `docs/CODING_STANDARDS.md` |
| Git conventions | `docs/GIT_CONVENTIONS.md` |
| Project map (files/modules not covered by convention) | `docs/PROJECT_MAP.md` |
| Manual follow-ups | `TODO.md` |
