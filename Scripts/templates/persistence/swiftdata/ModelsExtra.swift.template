
/// The stored form of `__FEATURE__Model`.
///
/// The record is the storage shape and the model is the domain shape; the local
/// store maps between them and nothing else traffics in records.
@Model
public final class __FEATURE__Record {
__RECORD_PROPERTIES__
    public init(from model: __FEATURE__Model) {
__RECORD_INIT_ASSIGN__
    }

    /// Converts this record back into its domain model.
    public func asModel() -> __FEATURE__Model {
        __FEATURE__Model(__RECORD_TO_MODEL_ARGS__)
    }
}
