import UIKit
__MODULE_IMPORTS__

/// Decides where the __FEATURE__ scene goes next.
///
/// Not to be confused with `AppCoordinator`, which owns the navigation stack for
/// the whole app. This type owns navigation *decisions* for one scene and hands
/// the actual push off to the Coordinator — it never manipulates the stack itself.
public struct __FEATURE__Router {
    private let coordinator: AppCoordinator

    public init(coordinator: AppCoordinator) {
        self.coordinator = coordinator
    }

    // Nowhere to navigate yet. Add a routeTo<Destination>() method here once
    // __FEATURE__ has a next screen, e.g.:
    //
    // func routeToDetail(for item: __FEATURE__Model) {
    //     coordinator.push(DetailViewController(item: item))
    // }
    //
    // TODO(agent/dev): wire this once __FEATURE__ has a next screen.
}
