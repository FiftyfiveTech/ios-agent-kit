/// Namespace for the structs that carry data between this feature's layers.
public enum __FEATURE__ {
    /// The load use case: what's asked for, what came back, what's shown.
    public enum Load {
        public struct Request {
            public init() {}
        }

        public struct Response {
            public let items: [__FEATURE__Model]

            public init(items: [__FEATURE__Model]) {
                self.items = items
            }
        }

        public struct ViewState {
            public let items: [__FEATURE__Model]

            public init(items: [__FEATURE__Model]) {
                self.items = items
            }
        }
    }
}
