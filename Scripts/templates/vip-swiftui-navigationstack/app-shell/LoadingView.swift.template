import SwiftUI

/// Loading state shown while a screen's data is in flight.
public struct LoadingView: View {
    public init() {}

    public var body: some View {
        ProgressView()
            .accessibilityLabel(Text(L10n.__MODULE_LOWER__.common.loading))
    }
}

#Preview {
    LoadingView()
}
