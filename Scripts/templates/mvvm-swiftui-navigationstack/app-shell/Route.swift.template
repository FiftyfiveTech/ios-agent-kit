import Foundation

// One Route case per screen the Router can push. SwiftUI `NavigationStack` + Router
// (§3.4) — the ONE navigation mechanism for the whole app (§3.4). /new-feature
// inserts a new case directly above the marker; it never introduces a second
// navigation path. Shipped with zero cases — /start populates the first one by
// running new_feature.sh for the starter feature, same as every /new-feature call.
public enum Route: Hashable {
    // MARK: new-feature-route-insertion-point
}
