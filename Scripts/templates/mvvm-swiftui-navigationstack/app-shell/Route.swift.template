import Foundation

/// Every screen the app can navigate to — one case per destination.
///
/// This is the app's single navigation vocabulary. `/new-feature` inserts a case
/// at the marker below; don't add one by hand and don't introduce a second
/// navigation mechanism alongside it.
public enum Route: Hashable {
    // MARK: new-feature-route-insertion-point
}
